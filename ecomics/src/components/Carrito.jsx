import * as React from "react";
import { useDispatch, useSelector } from "react-redux";

export default ()=>{


    return(
        <div>
            <h1>esto es un carrito</h1>
        </div>
    )
}